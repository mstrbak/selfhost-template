# selfhost-template
This project wants to makes the deployment of NixOS based servers as easy as possible. Currently only the Contabo VPS servers are supported. To deploy, fork this repo, add secrets and run the deployment actions. First run install if you have never deployed to the required server, it formats the disks, repartitions them and installs the NixOS.
