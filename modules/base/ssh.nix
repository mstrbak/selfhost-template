{ ... }:
{
  services.openssh = {
    enable = true;
    settings = {
      PasswordAuthentication = false;
      KbdInteractiveAuthentication = false;
      PermitRootLogin = "no";
      X11Forwarding = false;
    };
    # Bind explicitly to all interfaces; firewall controls public exposure.
    openFirewall = false;
  };
}
